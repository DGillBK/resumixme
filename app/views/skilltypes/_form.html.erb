<%= form_for(@skilltype) do |f| %>
  <% if @skilltype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skilltype.errors.count, "error") %> prohibited this skilltype from being saved:</h2>

      <ul>
      <% @skilltype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <%= f.hidden_field :user_id, :value => current_user.id%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
